{
  "private": false,
  "dependencies": {},
  "devDependencies": {},
  "scripts": {
    "install": "lerna bootstrap --hoist",
    "build": "lerna run build",
    "clean": "lerna run clean",
    "start:example": "lerna run start:example"
  }
}
